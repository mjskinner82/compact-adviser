{
  "name": "compact-adviser-claude-mod",
  "version": "0.1.4",
  "private": true,
  "license": "MIT",
  "description": "A standalone Claude Code mod that suggests semantic checkpoints for context compaction.",
  "type": "module",
  "scripts": {
    "types": "node scripts/plugin-types.mjs",
    "typecheck": "npm run types && tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "validate": "node scripts/validate.mjs",
    "check-readme": "node scripts/check-readme.mjs",
    "test": "node scripts/plugin-test.mjs",
    "check": "npm run typecheck && npm run lint && npm run validate && npm run check-readme && npm test",
    "test:e2e": "node scripts/live-e2e.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "typescript": "^5.9.0"
  }
}
